---
title: AI Recommended Prompts
description: Returns structured Korean analysis for all signal types in the Bithumb-KRW market in a single response — covers market reasons, asset moves, KOL themes, capital flows, technical signals, and undervalued assets.
openapi: openapi.json GET /v1/market/ai-recommended-prompts
---

<div style={{ maxWidth: 760 }}>

<h2 style={{ fontSize: "22px", marginBottom: 8 }}>Overview</h2>

<p style={{ fontSize: 15, lineHeight: 1.8 }}>
  The <strong>AI Recommended Prompts</strong> API returns structured Korean analysis across all
  <strong> 17 signal types</strong> covering the Bithumb-KRW market in a single call. Each item in the
  response array represents one signal, identified by its <code>type</code> field and rendered with a
  fixed canonical Korean title (never LLM-generated).
</p>

<Note>
  Refresh: **every 30 minutes**. Dagster cron `*/30 * * * *` KST. Promotion completes ≥10 min before each
  cadence mark (i.e. by `:20` and `:50`). The Bithumb FE displays 7 questions per session (3 fixed + 4
  randomized from the 14 variable types).
</Note>

<Warning>
  Signal types missing from the latest batch are **skipped** rather than returned as `null`. The array
  preserves a fixed canonical order (Q1 → Q17 below).
</Warning>

---

<h2 style={{ fontSize: "22px", marginBottom: 8 }}>Signal types (17)</h2>

Titles are **constants** (or templates with `{가상자산명}`) — never LLM-generated. Always render exactly as
shown in the table.

| # | `type` | Group | Canonical Korean title |
|---|---|---|---|
| 1 | `market_rise_fall_reason` | Fixed | `현재 시장의 상승/하락 이유는?` |
| 2 | `bullish_assets` | Fixed | `상승추세를 보이는 자산은?` |
| 3 | `trending_issues` | Fixed | `지금 시장에서 주목받는 이슈는?` |
| 4 | `asset_price_surge` | Market Issues & Trend | `{가상자산명} 상승 이유는?` |
| 5 | `asset_price_decline` | Market Issues & Trend | `{가상자산명} 하락 이유는?` |
| 6 | `kol_themes` | Market Issues & Trend | `최근 KOL들의 주요 관심 주제는?` |
| 7 | `kol_sectors` | Market Issues & Trend | `KOL들이 거론하는 섹터는?` |
| 8 | `positive_news_top5` | Market Issues & Trend | `오늘 가장 호재인 뉴스는?` |
| 9 | `hottest_meme_coins` | Market Issues & Trend | `가장 핫한 밈코인은?` |
| 10 | `exchange_outflows` | Market Continuance & Trends | `거래소에서 빠져나간 자산은?` |
| 11 | `stablecoin_inflows` | Market Continuance & Trends | `오늘 스테이블코인(USDT, USDC) 거래소 유입량은?` |
| 12 | `sector_leaders` | Market Continuance & Trends | `시장을 이끌고 있는 섹터는?` |
| 13 | `btc_etf_flow` | Market Continuance & Trends | `BTC ETF 자금 유입 추이는?` |
| 14 | `golden_cross` | Technical Trading Signals | `골든 크로스에 다가가는 종목은?` |
| 15 | `sentiment_divergence` | Technical Trading Signals | `감성 분기를 보이는 종목은?` |
| 16 | `top_active_chain` | Market Environment | `가장 활성화된 네트워크는?` |
| 17 | `undervalued_asset` | Market Environment | `시총 대비 저평가 자산은?` |

<Note>
  Q4 / Q5 templates render with the **Korean asset name**, not the symbol — e.g. `자이온 상승 이유는?`,
  not `XION 상승 이유는?` or `XION/KRW`.
</Note>

---

<h2 style={{ fontSize: "22px", marginBottom: 8 }}>Per-item Response Attributes</h2>

The response is an array; each item has the same shape and represents one signal type.

<ResponseField name="type" type="string" required>
  Signal type key. One of the 17 catalogue keys above.
</ResponseField>

<ResponseField name="report_time" type="string" required>
  Snapshot wall-clock for this signal's batch.
</ResponseField>

<ResponseField name="uid" type="string" required>
  Document identifier for this signal's batch.
</ResponseField>

<ResponseField name="title" type="string" required>
  Canonical Korean title from the catalogue above (or template-rendered for Q4/Q5). Never
  LLM-generated.
</ResponseField>

<ResponseField name="summary" type="string" required>
  Korean prose, multi-bullet markdown. ≤4–5 lines. Bullets separated by `\n- ` for FE-side splitting.
</ResponseField>

<ResponseField name="detailed_analysis" type="object" required>
  Structured Korean analysis content. The concrete key shape varies per question type — refer to the live
  response for the canonical fields returned by your `type`.
</ResponseField>

<ResponseField name="conclusion_neutral_opinion" type="string" required>
  **Neutral** Korean synthesis combining positive and cautionary signals.
</ResponseField>

<ResponseField name="conclusion_positive_factors" type="array of strings" required>
  Korean bullets — **positive evidence only**. No qualifications, no negative phrasing.
</ResponseField>

<ResponseField name="conclusion_cautionary_factors" type="array of strings" required>
  Korean bullets — **cautionary evidence only**. No positive phrasing leaks.
</ResponseField>

<ResponseField name="feedback" type="object">
  Common feedback metadata block.
</ResponseField>

<ResponseField name="share" type="object">
  Common share metadata block.
</ResponseField>

---

</div>

<RequestExample>

```bash cURL
curl --request GET \
  --url "https://api.asksurf.ai/surf-ai/v1/market/ai-recommended-prompts" \
  --header "Authorization: <api-key>"
```

```python Python
import requests

response = requests.get(
    "https://api.asksurf.ai/surf-ai/v1/market/ai-recommended-prompts",
    headers={"Authorization": "<api-key>"},
)
prompts = response.json()
```

```javascript JavaScript
const response = await fetch(
  "https://api.asksurf.ai/surf-ai/v1/market/ai-recommended-prompts",
  { headers: { Authorization: "<api-key>" } },
);
const prompts = await response.json();
```

</RequestExample>

